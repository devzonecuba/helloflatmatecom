{% extends 'config.html.twig' %}

{% block title %}Servicios{% endblock %}

{% block body %}
    <h1>Servicios</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ servicio.id }}</td>
            </tr>
            <tr>
                <th>Servicio</th>
                <td>{{ servicio.servicio }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('servicios_index') }}">back to list</a>

    <a href="{{ path('servicios_edit', {'id': servicio.id}) }}">edit</a>

    {{ include('servicios/_delete_form.html.twig') }}
{% endblock %}

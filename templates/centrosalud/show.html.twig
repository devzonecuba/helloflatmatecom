{% extends 'config.html.twig' %}

{% block title %}Centrosalud{% endblock %}

{% block body %}
    <h1>Centrosalud</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ centrosalud.id }}</td>
            </tr>
            <tr>
                <th>Nombrecentro</th>
                <td>{{ centrosalud.nombrecentro }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('centrosalud_index') }}">back to list</a>

    <a href="{{ path('centrosalud_edit', {'id': centrosalud.id}) }}">edit</a>

    {{ include('centrosalud/_delete_form.html.twig') }}
{% endblock %}

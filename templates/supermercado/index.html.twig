{% extends 'config.html.twig' %}

{% block title %}Supermercado index{% endblock %}

{% block body %}
    <h1>Supermercado index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombresuper</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for supermercado in supermercados %}
            <tr>
                <td>{{ supermercado.id }}</td>
                <td>{{ supermercado.nombresuper }}</td>
                <td>
                    <a href="{{ path('supermercado_show', {'id': supermercado.id}) }}">show</a>
                    <a href="{{ path('supermercado_edit', {'id': supermercado.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('supermercado_new') }}">Create new</a>
{% endblock %}

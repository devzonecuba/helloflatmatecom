{% extends 'config.html.twig' %}

{% block title %}Zona{% endblock %}

{% block body %}
    <h1>Zona</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zona.id }}</td>
            </tr>
            <tr>
                <th>Nombrezona</th>
                <td>{{ zona.nombrezona }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('zona_index') }}">back to list</a>

    <a href="{{ path('zona_edit', {'id': zona.id}) }}">edit</a>

    {{ include('zona/_delete_form.html.twig') }}
{% endblock %}

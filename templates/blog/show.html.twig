{% extends 'config.html.twig' %}

{% block title %}Blog{% endblock %}

{% block body %}
    <h1>Blog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blog.id }}</td>
            </tr>
            <tr>
                <th>Nombrecliente</th>
                <td>{{ blog.nombrecliente }}</td>
            </tr>
            <tr>
                <th>Asunto</th>
                <td>{{ blog.asunto }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ blog.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('blog_index') }}">back to list</a>

    <a href="{{ path('blog_edit', {'id': blog.id}) }}">edit</a>

    {{ include('blog/_delete_form.html.twig') }}
{% endblock %}
